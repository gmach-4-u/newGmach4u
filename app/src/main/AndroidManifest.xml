<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gmach4u">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gmach4U">
        <activity android:name=".Product"></activity>
        <activity android:name=".SearchHistory" />
        <activity android:name=".GmachStockSupplier" />
        <activity android:name=".GmachCustomers" />
        <activity android:name=".Chat" />
        <activity android:name=".GmachStockClient" />
        <activity android:name=".GmachDetails" />
        <activity android:name=".SearchResults" />
        <activity android:name=".PrivateZone" />
        <activity android:name=".MainSupplier" />
        <activity android:name=".SupplierRegister" />
        <activity android:name=".ClientRegister" /> <!-- <activity android:name=".clientRegister" /> -->
        <activity android:name=".MainActivity" />
        <activity android:name=".loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>